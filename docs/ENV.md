# Environment variables

See the root **[README.md](../README.md#variables-de-entorno)** for the full list.

```bash
cp .env.example .env
cp web/.env.example web/.env
```

Never commit `.env` — it is in `.gitignore`.
